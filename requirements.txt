# Core ML / Deep Learning
# Pinned to match the exact version used to train/save the .keras models
# (Keras 3 serializes layer configs with fields like quantization_config
# that older Keras/TF versions cannot deserialize).
tensorflow==2.21.0
scikit-learn>=1.3.0
xgboost>=2.0.0

# Pose estimation (≥0.10.14 usa la nueva Tasks API; mp.solutions eliminado)
mediapipe>=0.10.14

# Image processing
opencv-python>=4.8.0
Pillow>=10.0.0
albumentations>=1.3.0

# Data / Numerics
numpy>=1.24.0
pandas>=2.0.0

# Visualization & Explainability
matplotlib>=3.7.0
seaborn>=0.12.0
shap>=0.43.0

# Utilities
tqdm>=4.65.0

# Web demo
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
